
spring:
  datasource:
    url: jdbc:postgresql://172.23.240.1:5432/absence_db
    username: postgres
    password: skasequerin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate          # schema created by Docker init scripts
    show-sql: false
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: ${SERVER_PORT:8080}

springdoc:
  api-docs.path: /api-docs
  swagger-ui.path: /swagger-ui.html

absence:
  threshold: ${ABSENCE_THRESHOLD:33}